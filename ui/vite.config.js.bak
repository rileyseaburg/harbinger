import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// bundle for tauri  

export default defineConfig({
  plugins: [react()],
  server: {
    port: 1420,
    strictPort: true,
  },
  build: {
    target: 'esnext',
    minify: 'esbuild',
  },
  build: {
    rollupOptions: {
      external: []
    }
  }
})